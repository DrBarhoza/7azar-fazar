# Only the generator scripts need these. The game itself (index.html) needs
# nothing — open it in a browser and load a bank from the Question Bank panel.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   .venv/bin/python gen/barahma_gen.py
#
# Pillow  — gen/barahma_gen.py, gen/landmark_photos_gen.py (photo resize/optimize)
# pypdf   — gen/emoji_pdf.py (parse the emoji PDF)
Pillow==11.3.0
pypdf==6.14.2
